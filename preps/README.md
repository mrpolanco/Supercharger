# Job preps

Each folder is one job preparation pack. Create one from the app ("New job
prep" — saves `job-posting.md`), then ask Claude Code to generate the rest:
`analysis.md`, `plan.md`, `interview-prep.md`. See ../SPEC.md.
